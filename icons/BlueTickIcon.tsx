import { Text } from "@chakra-ui/react";
import React from "react";

const BlueTickIcon: React.FC = (props) => {
  return (
    <div>
      <Text fontWeight={"bold"}>Connect</Text>
    </div>
  );
};

export default BlueTickIcon;

